export enum FontSizeClassEnum {
    SM = 'text-sm',
    BASE = 'text-base',
    LG = 'text-lg',
    XL = 'text-xl',
    XXL = 'text-2xl',
}

export enum FontSizeValueEnum {
    SM = 0,
    BASE = 25,
    LG = 50,
    XL = 75,
    XXL = 100,
}